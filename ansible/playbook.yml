---
- name: Deploy php App
  hosts: all
  gather_facts: false

  tasks:
    - name: Apply Deployment
      k8s:
        definition: "{{ lookup('file', 'manifest.yml') }}"
        state: present

    - name: Apply Service
      k8s:
        definition: "{{ lookup('file', 'service.yml') }}"
        state: present
